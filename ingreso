const limpiarItem = (valor: string) => {
  if (!valor) return valor;
  return valor.split(';')[0].trim();
};

const normalizar = (v: string) =>
  limpiarItem(v)
    .replace(/\s+/g, '')
    .toUpperCase();