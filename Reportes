return {
  Codigo: '200',
  Message:
    tipoIngreso.toLowerCase() === 'parcial'
      ? totalIngresosGenerados === 0
        ? '0 ingresos no están completos'
        : `Se generaron ${totalIngresosGenerados} ingresos`
      : 'Consulta realizada con éxito',
  totalIngresosGenerados,
  asig_numero
};