let filaInicioDatos = 2; // porque tienes 2 encabezados

data.forEach((_, index) => {
  const fila = filaInicioDatos + index;

  ws["!merges"].push(
    { s: { r: fila, c: 0 }, e: { r: fila, c: 1 } }, // Nombre
    { s: { r: fila, c: 4 }, e: { r: fila, c: 6 } }  // Descripción
  );
});