const consulta2 = await this.prismaNovasoftService.$queryRawUnsafe<any[]>(`
  SELECT SUM(cantidad) AS cantidad
  FROM inv_inf_inv 
  WHERE obs_orc LIKE '%' + '${Referencia}' + '%' 
    AND asig_numero = ${usr_pedido}
`);


let estado = 0;

if (
  cantidad1 !== null &&
  cantidad2 !== null &&
  cantidad1 === cantidad2
) {
  estado = 1;
}
