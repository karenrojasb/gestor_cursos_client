
return {
  Codigo: '200',
  
  Message:
    tipoIngreso.toLowerCase() === 'parcial'
      ? totalIngresosGenerados === 0
        ? 'Los Ingresos no están completos'
        : `Ya hay ingresos generados`
      : 'Consulta realizada con éxito',
  
  asig_numero
};
