let obsIndex = 7;

columnas.forEach(() => {
  // 🔹 Merge en encabezado (Observaciones)
  ws["!merges"]!.push({
    s: { r: 1, c: obsIndex + 7 },
    e: { r: 1, c: obsIndex + 8 }
  });

  // 🔹 Merge en filas de datos
  for (let i = 2; i < wsData.length; i++) {
    ws["!merges"]!.push({
      s: { r: i, c: obsIndex + 7 },
      e: { r: i, c: obsIndex + 8 }
    });
  }

  obsIndex += 13;
});