if (Number(cantidad_Nova) !== Number(nova.cantidad)) {
  throw new BadRequestException(
    `La cantidad ingresada (${nova.cantidad}) no coincide con la cantidad registrada en Nova (${cantidad_Nova}). El consecutivo no puede generarse.`
  );
}