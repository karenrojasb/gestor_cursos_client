let obsIndex = 7;

columnas.forEach(() => {
  // 🔹 HEADER (fila 1)
  ws["!merges"]!.push({
    s: { r: 1, c: obsIndex + 7 },
    e: { r: 1, c: obsIndex + 8 }
  });

  // 🔹 FILAS DE DATOS
  for (let i = 2; i < wsData.length; i++) {
    ws["!merges"]!.push({
      s: { r: i, c: obsIndex + 7 },
      e: { r: i, c: obsIndex + 8 }
    });
  }

  obsIndex += 13;
});