const lista = grupo.Proveedores || [];

const listaProveedor = lista.filter(
  (p: any) => p.Proveedor === col.proveedor
);

// 👇 GARANTIZA posición aunque falte info
const info = listaProveedor[col.index] ?? {};