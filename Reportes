const consulta1 = await this.prismaNovasoftService.$queryRawUnsafe<any[]>(`
  SELECT SUM(cantidad) AS cantidad
  FROM inv_inf_inv 
  WHERE obs_orc LIKE '%' + '${Referencia}' + '%' 
    AND usr_pedido = ${usr_pedido}
`);