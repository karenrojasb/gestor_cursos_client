 const tieneDato = (val: any) =>
  val !== null && val !== undefined && String(val).trim() !== '';



let estado = 0;

if (
  valoresEstado.length > 0 &&
  valoresEstado.every(tieneDato)
) {
  estado = 1;
}