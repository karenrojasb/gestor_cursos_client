const tar_rii = `${data.cliente} (Con el NIT ${provee}) Valor campo ret_iva: ${ret_iva}`;


const tar_rii = `${nombre_proveedor || data.cliente} (Con el NIT ${provee}) Valor campo ret_iva: ${ret_iva}`;
const tar_rii_ng = `${nombre_proveedor || data.cliente} (Con el NIT ${provee}) Valor campo ret_iva_ng: ${ret_iva_ng}`;